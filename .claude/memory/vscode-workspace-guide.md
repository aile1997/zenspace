# VS Code 工作区使用指南

> **更新时间**: 2026-01-24
> **维护者**: Architect
> **适用于**: 前后端开发者

---

## 📐 架构优化说明

ZenSpace 采用 **Monorepo 架构**，前后端代码在同一个 Git 仓库中，但通过 **VS Code 工作区**实现清晰的逻辑分区。

### 优化前 vs 优化后

| 维度 | 优化前 ❌ | 优化后 ✅ |
|:-----|:---------|:---------|
| **源代码管理** | 前后端混杂在一起 | 按目录清晰分组 |
| **提交粒度** | 一次提交包含前后端 | 前后端分开提交 |
| **开发体验** | 需要频繁切换目录 | 多工作区并行开发 |
| **代码审查** | 难以区分前后端改动 | PR 按模块清晰呈现 |
| **CI 触发** | 全量运行 | 按目录触发 |

---

## 🚀 快速开始

### 1️⃣ 打开工作区

**方法一：命令行启动**

```bash
# 进入项目目录
cd /path/to/zenspace

# 使用工作区文件打开
code zenspace.code-workspace
```

**方法二：VS Code 内打开**

```
File → Open Workspace from File... → 选择 zenspace.code-workspace
```

### 2️⃣ 工作区布局

打开工作区后，你会看到 **5 个独立的文件夹**：

```
📂 ZenSpace 工作区
  ├── 🎨 Frontend - Core (业务逻辑层)
  ├── 🎨 Frontend - UI (视图层)
  ├── ⚙️ Backend (后端服务)
  ├── 📐 Claude AI (架构系统)
  └── 📦 Root (根目录配置)
```

**每个文件夹都是独立的工作区根目录**，相当于同时打开了 5 个项目！

---

## 📂 工作区结构详解

### 🎨 Frontend - Core (业务逻辑层)

**路径**: `frontend/packages/core`

**职责**:
- 业务逻辑（Composables、Stores）
- 类型定义（Types）
- 平台接口抽象（Adapters）
- 单元测试（Tests）

**常用命令**:
```bash
pnpm run test          # 运行测试
pnpm run test:watch    # 监听模式
pnpm run type-check    # 类型检查
```

**注意**:
- ✅ 100% 纯 TypeScript
- ❌ 禁止使用 `uni.xxx`、`wx.xxx` 等平台 API

---

### 🎨 Frontend - UI (视图层)

**路径**: `frontend/packages/ui`

**职责**:
- Vue 页面和组件
- UniApp 配置（pages.json）
- 平台适配器实现
- 静态资源

**常用命令**:
```bash
pnpm run dev:h5           # H5 开发
pnpm run dev:mp-weixin    # 微信小程序
pnpm run build:h5         # 构建 H5
```

**注意**:
- ✅ 可以使用平台 API (在 adapters 中)
- ✅ 引用 Core 层逻辑

---

### ⚙️ Backend (后端服务)

**路径**: `backend`

**职责**:
- NestJS 应用
- Prisma 数据库模型
- API 路由和服务
- 单元和集成测试

**常用命令**:
```bash
pnpm run start:dev     # 开发模式
pnpm run test          # 运行测试
pnpm prisma studio     # 打开数据库管理界面
pnpm prisma migrate dev # 运行数据库迁移
```

---

### 📐 Claude AI (架构系统)

**路径**: `.claude`

**职责**:
- 架构文档（memory/）
- 产品需求（contracts/）
- 开发工票（tickets/）
- AI 技能包（skills/）

**只读访问** - 主要用于查阅文档

---

### 📦 Root (根目录配置)

**路径**: `.`

**职责**:
- 根级配置文件
- Git 配置
- CI/CD 配置
- Husky Hooks

**注意**: 这个工作区包含所有文件，仅用于管理全局配置

---

## 🔄 源代码管理（Git）分区

### 📊 Source Control 面板自动分组

在 VS Code 的源代码管理面板中，改动会按目录自动分组：

```
Changes (5)
  ├── frontend/packages/core (2)
  │   ├── src/composables/useAuth.ts
  │   └── tests/composables/useAuth.spec.ts
  ├── frontend/packages/ui (1)
  │   └── src/pages/login/index.vue
  └── backend (2)
      ├── src/auth/auth.service.ts
      └── src/auth/auth.controller.ts
```

### ✅ 推荐的提交策略

#### 原则 1: 前后端分开提交

```bash
# ❌ 错误示范 - 一次提交包含前后端
git add .
git commit -m "实现登录功能"

# ✅ 正确示范 - 分开提交
# 第一次提交：前端
git add frontend/
git commit -m "feat(ui): 添加登录页面 UI"

# 第二次提交：后端
git add backend/
git commit -m "feat(backend): 实现用户认证 API"
```

#### 原则 2: 使用约定式提交

**格式**:
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Scope 规范**:

| Scope | 说明 | 示例 |
|:------|:-----|:-----|
| `ui` | 前端 UI 层 | `feat(ui): 添加选座页面` |
| `core` | 前端 Core 层 | `feat(core): 实现预约逻辑` |
| `backend` | 后端服务 | `feat(backend): 添加座位 API` |
| `auth` | 认证模块（前后端通用） | `fix(auth): 修复 Token 刷新` |
| `booking` | 预约模块 | `feat(booking): 添加签到功能` |
| `infra` | 基础设施 | `chore(infra): 更新 CI 配置` |

**示例提交**:

```bash
# 前端 UI 改动
git commit -m "feat(ui): 完成首页 UI 设计

- 添加 Glassmorphism 风格卡片
- 实现区域卡片渐变背景
- 添加拥挤度进度条

Related to #4"

# 前端逻辑改动
git commit -m "feat(core): 实现预约创建逻辑

- 添加 createBooking 方法
- 集成支付流程
- 添加单元测试

Related to #4"

# 后端改动
git commit -m "feat(backend): 实现预约管理 API

- POST /api/bookings - 创建预约
- GET /api/bookings - 获取预约列表
- PUT /api/bookings/:id/cancel - 取消预约

Related to #2"
```

### 🎯 在 VS Code 中分区提交

**步骤**:

1. **打开 Source Control 面板** (Ctrl/Cmd + Shift + G)

2. **选择要提交的文件**
   - 只勾选 `frontend/` 下的文件
   - 或只勾选 `backend/` 下的文件

3. **写提交消息**
   - 确保 scope 正确（ui / core / backend）

4. **点击 Commit**

5. **重复步骤** 提交其他部分

### 📌 使用 GitLens 增强（推荐）

安装 GitLens 扩展后，可以：
- 查看每一行代码的提交历史
- 按文件夹过滤提交记录
- 可视化分支和提交图

---

## 🛠️ 内置任务（Tasks）

工作区配置了常用的开发任务，可以快速启动。

### 如何运行任务

**方法一：命令面板**
```
Ctrl/Cmd + Shift + P
→ Tasks: Run Task
→ 选择任务
```

**方法二：终端菜单**
```
Terminal → Run Task...
```

### 可用任务清单

#### 🎨 前端任务

| 任务名 | 说明 | 快捷操作 |
|:------|:-----|:---------|
| `🎨 Frontend: Install` | 安装前端依赖 | - |
| `🎨 Frontend: Dev (H5)` | 启动 H5 开发服务器 | ⭐ 常用 |
| `🎨 Frontend: Test` | 运行前端测试 | ⭐ 常用 |

#### ⚙️ 后端任务

| 任务名 | 说明 | 快捷操作 |
|:------|:-----|:---------|
| `⚙️ Backend: Install` | 安装后端依赖 | - |
| `⚙️ Backend: Dev` | 启动后端开发服务器 | ⭐ 常用 |
| `⚙️ Backend: Test` | 运行后端测试 | ⭐ 常用 |

#### 🚀 组合任务

| 任务名 | 说明 | 包含任务 |
|:------|:-----|:---------|
| `🚀 Full Stack: Install All` | 安装所有依赖 | 前端 + 后端 |
| `🚀 Full Stack: Dev` | 同时启动前后端 | 前端 H5 + 后端 |

### 快捷启动全栈开发

```bash
# 运行组合任务
Tasks: Run Task → 🚀 Full Stack: Dev
```

会同时启动：
- 前端 H5 开发服务器 (http://localhost:5173)
- 后端 API 服务器 (http://localhost:3000)

---

## 🐛 调试配置（Launch Configurations）

工作区配置了多种调试配置，可以直接使用。

### 可用调试配置

#### 1️⃣ 🎨 Frontend - H5 Dev

**用途**: 在 Chrome 中调试前端 H5 应用

**启动**:
```
Run and Debug (Ctrl/Cmd + Shift + D)
→ 选择 "🎨 Frontend - H5 Dev"
→ 点击绿色播放按钮
```

**注意**: 需要先运行 `pnpm run dev:h5`

#### 2️⃣ ⚙️ Backend - Debug

**用途**: 调试 NestJS 后端服务

**启动**:
```
Run and Debug
→ 选择 "⚙️ Backend - Debug"
→ 点击播放
```

**功能**:
- 断点调试
- 变量查看
- 调用栈
- 自动重启（nodemon）

#### 3️⃣ 🧪 Frontend - Core Tests

**用途**: 调试前端 Core 层的单元测试

**启动**:
```
Run and Debug
→ 选择 "🧪 Frontend - Core Tests"
→ 点击播放
```

#### 4️⃣ 🚀 Full Stack Debug（组合）

**用途**: 同时调试前后端

**启动**:
```
Run and Debug
→ 选择 "🚀 Full Stack Debug"
→ 点击播放
```

会同时启动前端和后端调试会话！

---

## 🎨 终端管理技巧

### 推荐的终端布局

**终端 1: 前端 Core**
```bash
cd frontend/packages/core
pnpm run test:watch   # 监听测试
```

**终端 2: 前端 UI**
```bash
cd frontend/packages/ui
pnpm run dev:h5       # H5 开发服务器
```

**终端 3: 后端**
```bash
cd backend
pnpm run start:dev    # 后端开发服务器
```

**终端 4: Git**
```bash
# 根目录，用于 Git 操作
git status
git add ...
git commit ...
```

### 快捷键

| 快捷键 | 功能 |
|:------|:-----|
| `Ctrl/Cmd + ~` | 打开/关闭终端 |
| `Ctrl/Cmd + Shift + ~` | 创建新终端 |
| `Ctrl/Cmd + \`` | 在终端间切换 |

---

## 📦 共享类型包（未来优化）

### 当前状态

前后端类型定义在各自的目录中：
- 前端：`frontend/packages/core/src/types/`
- 后端：`backend/src/` (由 Prisma 生成)

### 未来优化方案（可选）

创建共享类型包 `packages/types/`:

```
zenspace/
├── packages/
│   └── types/              # 前后端共享类型
│       ├── user.ts
│       ├── booking.ts
│       ├── seat.ts
│       └── zone.ts
├── frontend/
│   └── packages/
│       ├── core/           # 引用 @zenspace/types
│       └── ui/
└── backend/                # 引用 @zenspace/types
```

**优势**:
- ✅ 类型定义单一来源
- ✅ 消灭前后端接口不一致
- ✅ 一次定义，两端复用

**实施时机**: 当前阶段不急，等 MVP 完成后再优化

---

## 🔧 工程化工具（未来）

### pnpm workspaces

已配置 pnpm workspaces，支持：
- 依赖提升
- 符号链接
- 快速安装

### 未来可集成的工具

| 工具 | 用途 | 优先级 |
|:-----|:-----|:------|
| **Turbo** | Monorepo 构建加速 | P1 |
| **Changesets** | 版本管理和发布 | P2 |
| **Nx** | 增量构建和缓存 | P2 |

---

## 💡 最佳实践

### ✅ 推荐做法

1. **使用工作区文件打开项目**
   ```bash
   code zenspace.code-workspace
   ```

2. **前后端分开提交**
   ```bash
   git add frontend/
   git commit -m "feat(ui): ..."

   git add backend/
   git commit -m "feat(backend): ..."
   ```

3. **使用内置任务启动服务**
   ```
   Tasks: Run Task → 🚀 Full Stack: Dev
   ```

4. **使用调试配置而非 console.log**
   ```
   Run and Debug → 🚀 Full Stack Debug
   ```

5. **合理使用多终端**
   - 终端 1: 前端开发
   - 终端 2: 后端开发
   - 终端 3: Git 操作
   - 终端 4: 数据库/其他

### ❌ 避免做法

1. ❌ 直接打开根目录（不使用工作区）
2. ❌ 前后端混在一起提交
3. ❌ 提交消息不写 scope
4. ❌ 在 Core 层使用平台 API
5. ❌ 手动切换目录（应该使用工作区切换）

---

## 🐛 常见问题

### Q1: 为什么要用工作区而不是直接打开根目录？

**A**: 工作区的优势：
- ✅ 前后端逻辑分区更清晰
- ✅ 源代码管理按目录自动分组
- ✅ 每个文件夹有独立的设置
- ✅ 支持多根目录调试
- ✅ 终端默认路径自动设置

### Q2: 工作区文件可以放到 Git 吗？

**A**: 可以！`zenspace.code-workspace` 已加入 Git，团队成员可以直接使用。

### Q3: 如何快速切换工作区根目录？

**A**: 点击左下角的文件夹图标，选择要切换的工作区。

### Q4: 前后端能同时调试吗？

**A**: 可以！使用 `🚀 Full Stack Debug` 组合调试配置。

### Q5: 提交时如何只选择前端或后端文件？

**A**: 在 Source Control 面板中，只勾选对应目录的文件即可。

---

## 📚 相关文档

- [VS Code 工作区文档](https://code.visualstudio.com/docs/editor/workspaces)
- [GitLens 扩展](https://marketplace.visualstudio.com/items?itemName=eamodio.gitlens)
- [约定式提交规范](https://www.conventionalcommits.org/zh-hans/)
- [pnpm workspaces](https://pnpm.io/workspaces)

---

## 🆘 需要帮助？

遇到问题请查阅：
- `.claude/memory/architecture_overview.md` - 架构总览
- `.github/README.md` - 开发规范

---

**文档维护者**: Architect
**最后更新**: 2026-01-24
**适用版本**: VS Code 1.85+
